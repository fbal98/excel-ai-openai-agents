openai-agents
openpyxl
python-dotenv

# Realtime single‑char keyboard input (Win / *nix)
getchlib>=0.3.1
# Optional but recommended for rich prompt history / cursor movement
prompt_toolkit>=3.0.50
xlwings>=0.30.0
rich